# Sift Mail Desktop

Electron kabuğu Windows, macOS ve Linux için ortak `core/` sunucusunu ve `shared/` arayüzünü paketler.

- Geliştirme: `npm run dev:desktop`
- Paketleme: `npm run build:win`, `npm run build:mac` veya `npm run build:linux`
